import { DropZoneProps } from '../types';

/**
 * Drop zone component for drag-and-drop file uploads
 */
export declare function DropZone({ onFiles, accept, disabled, className, children, isDragging: externalIsDragging, }: DropZoneProps): import("react/jsx-runtime").JSX.Element;
